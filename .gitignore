# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*
!.env.example

# Supabase CLI scratch
supabase/.temp/

# Machine-local dev-server launch config (see .claude/launch.example.json)
.claude/launch.json

# Share images are rendered at deploy time (npm run assets) — committing them
# would put a couple of megabytes of GIF into history on every data change.
public/share/
